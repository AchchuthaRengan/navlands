import { describe, expect, it } from "vitest";

describe("rls bootstrap placeholder", () => {
  it("reserves the rls test suite for the hosted-dev schema checkpoint", () => {
    expect(true).toBe(true);
  });
});
