Review the M1 AI gateway for contract fidelity and fallback safety.

Focus on:

- mock mode as the required default
- shared provider abstraction only
- logging to `ai_call_log`
- budget tracking via `ai_daily_budget`
- contract validation against shared schemas
- anonymous rate limiting integration

Do not suggest M2 AI product features.
