# Evals

M1 keeps evals lightweight and contract-focused.

- `mock/generate-path-input.json` drives the shared AI gateway mock fixture.
- Unit tests validate the returned payloads against the shared schemas.
- These evals intentionally stay within M1 scope and do not introduce onboarding, canvas, or M2 product work.
